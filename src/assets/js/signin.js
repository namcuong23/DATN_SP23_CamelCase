const btnEye = document.getElementsByClassName('eye')
const inputType = document.getElementById("password")

btnEye.addEventListener('click', () => {
    console.log('1');
    // inputType.type == "password" ? inputType.type == "text" : inputType.type == "password"
})